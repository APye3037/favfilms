# Copy this file to .env.local (at the repo root or inside /web, matching
# wherever the Vite app loads its env from) and fill in real values.
# Never commit the real .env.local file — it is covered by .gitignore.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
